const config = {
  theme: {
    fontFamily: {
      sans: ["Rubik", "system-ui", "-apple-system", "Segoe UI", "sans-serif"],
    },
    extend: {
      colors: {
        primary: "#0177FF",
      },
    },
  },
};

export default config;
